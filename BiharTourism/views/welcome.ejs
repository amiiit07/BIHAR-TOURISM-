<!DOCTYPE html>
<html>
<head>
  <title>Welcome to Bihar</title>
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:wght@600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      text-align: center;
      background: linear-gradient(135deg, #ff9933, #ffffff, #138808); /* Tricolor Gradient */
      font-family: 'Poppins', sans-serif;
      color: #222;
    }
    h1 {
      font-size: 4rem;
      font-family: 'Pacifico', cursive;
      animation: fadeIn 2s ease-in-out;
      text-shadow: 2px 2px 6px rgba(0,0,0,0.3);
      color: #ff6666; /* Light Red */
    }
    p.tagline {
      font-size: 1.5rem;
      margin-bottom: 20px;
      animation: fadeIn 3s ease-in-out;
      color: #333;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .btn {
      margin: 15px;
      padding: 12px 25px;
      font-size: 1.2rem;
      border: none;
      border-radius: 30px;
      background: #004aad; /* Royal Blue Button */
      color: white;
      cursor: pointer;
      transition: 0.3s;
      text-decoration: none;
      display: inline-block;
      box-shadow: 2px 2px 8px rgba(0,0,0,0.4);
    }
    .btn:hover {
      background: #002f6c;
      transform: scale(1.1);
    }
  </style>
</head>
<body>
  <div>
    <h1>✨ Welcome to Bihar ✨</h1>
    <p class="tagline">Land of History, Culture & Spirituality</p>

    <a href="/culture" class="btn"><i class="fas fa-landmark"></i> Explore Culture</a>
    <a href="/destinations" class="btn"><i class="fas fa-map-marked-alt"></i> Famous Places</a>
    <a href="/cuisine" class="btn"><i class="fas fa-utensils"></i> Cuisine</a>
  </div>

  <%- include("partials/footer") %>
</body>
</html>
